import {Box, Typography} from '@mui/material';


export default function NotFound(){
    return(
        <Box sx={{textAlign:'center',mt:30}}>
            <Typography variant='h1'>Page Not Found</Typography>
            {/* <Button variant='contained'>Connect Metamsk</Button> */}
        </Box>
    );
}